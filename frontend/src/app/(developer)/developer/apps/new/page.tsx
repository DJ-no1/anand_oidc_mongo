export default function NewDeveloperAppPage() {
  return (
    <main className="mx-auto flex min-h-full max-w-3xl flex-col gap-6 px-6 py-16">
      <h1 className="text-xl font-semibold tracking-tight">New application</h1>
      <p className="text-sm text-muted-foreground">
        Placeholder. Form will post to the backend client registration API with
        redirect URI validation.
      </p>
    </main>
  );
}
